{
  "name": "lean-scale-ai-support-poc",
  "version": "1.0.0",
  "description": "AI-first customer support chatbot POC for the Lean Scale case study.",
  "type": "module",
  "scripts": {
    "build": "esbuild src/client/main.jsx --bundle --outfile=public/app.js --format=esm --jsx=automatic",
    "start": "npm run build && node src/server.js",
    "test": "node --test"
  },
  "dependencies": {
    "esbuild": "^0.27.4",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  }
}
